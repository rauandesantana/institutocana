name: institutocana
description: Instituto Caná para a Família.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/imagens/

  fonts:
    - family: Kaczun Oldstyle
      fonts:
         - asset: assets/fontes/kaczun_oldstyle.ttf
           style: italic